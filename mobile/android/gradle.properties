org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin incremental compilation repeatedly corrupts its caches for the vendored
# ar_flutter_plugin_2 module (phantom "Unresolved reference: deserializeMatrix4"
# errors on the first build after editing ArView.kt). Disable it.
kotlin.incremental=false
