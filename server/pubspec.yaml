name: genai_server
description: Local Meshy proxy for the genai Flutter AR app.
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
